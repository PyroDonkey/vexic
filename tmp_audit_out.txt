# RELEASE AUDIT DUMP (reconstructed — Shell Rejected in this subagent)
# Method: .git/refs for SHAs; reflog for commit subjects; installed uv tool
# package vexic 0.1.6 as origin/main tree for Python sources; working tree /
# agent-tools cached diffs for recorder/cli/prime/tests.
# NOT byte-identical to `git diff` hunk headers/index lines, but content delta
# for Python sources matches main(0.1.6) → dev(0.1.7).

=== git rev-parse origin/main origin/dev HEAD ===
281d02ff00813c1cf3243f71545c7befe6d2f692
4c19e348ed876602a0dfdf884b99ab96fef98f35
4c19e348ed876602a0dfdf884b99ab96fef98f35

=== git log --oneline origin/main..dev (RECONSTRUCTED from reflogs; first-parent tips + known subjects) ===
4c19e34 chore: bump version to 0.1.7 and refresh README status
b05026d [merge/ff into dev — full subject requires git object decompress]
7e70e88 docs: add dreaming and recorder terms to product glossary
538ff25 [merge/ff into dev — full subject requires git object decompress]
9870af9 docs: note bounded prelude retry in hosted-mvp sweeper section (ADR 0030 amendment)
bae18f5 [merge/ff into dev — full subject requires git object decompress]
# NOTE: the three [merge/ff] commits likely squash-contain the recorder
# deadline/retry/overlap + mcp_http ValidationError sanitize feature work.
# Full `git log --oneline origin/main..dev` needs zlib decompress of commit
# objects (Shell blocked; Read blocks binary .git/objects).

=== git diff --name-only origin/main...dev (RECONSTRUCTED — incomplete for docs) ===
README.md
pyproject.toml
src/vexic/__init__.py
src/vexic/mcp_http.py
src/vexic/recorders/cli.py
src/vexic/recorders/hosted_ingest.py
src/vexic/recorders/hosted_prime.py
src/vexic/recorders/status.py
tests/test_claude_code_recorder_cli.py
tests/test_claude_code_recorder_cursor.py
tests/test_mcp_http.py
tests/test_packaging_surface.py
docs/ai/CONTEXT.md
docs/hosted-mvp.md
# (additional docs may exist in the ff merges — need full tree walk)

=== git diff --name-only origin/main...dev -- tests/** ===
tests/test_claude_code_recorder_cli.py
tests/test_claude_code_recorder_cursor.py
tests/test_mcp_http.py
tests/test_packaging_surface.py
# test_hosted_http.py matched validation_error patterns but may already be on main

=== FULL DIFF: src/vexic/mcp_http.py (0.1.6 package → working tree) ===
diff --git a/src/vexic/mcp_http.py b/src/vexic/mcp_http.py
--- a/src/vexic/mcp_http.py
+++ b/src/vexic/mcp_http.py
@@ -16,6 +16,7 @@ from vexic.contract import (
     SearchTranscriptRequest,
     TrustBoundary,
 )
+from vexic.error_reporting import validation_error_message
 from vexic.hosted import HostedAuthContext, HostedMemoryService, HostedRateLimitExceeded
 from vexic.mcp_presentation import (
     RECALL_CONVERSATION_HISTORY,
@@ -283,6 +284,10 @@ async def _call_tool(
         # ValueErrors are safe JSON-RPC tool errors.
         if _is_storage_error(exc):
             raise
+        if isinstance(exc, ValidationError):
+            # str(ValidationError) embeds the offending input value and a
+            # pydantic docs URL; keep only the validator-authored messages.
+            return _tool_error(validation_error_message(exc))
         return _tool_error(str(exc))
     except Exception as exc:
         if _is_storage_error(exc):
             raise

=== FULL DIFF: src/vexic/recorders/status.py (0.1.6 package → working tree) ===
diff --git a/src/vexic/recorders/status.py b/src/vexic/recorders/status.py
--- a/src/vexic/recorders/status.py
+++ b/src/vexic/recorders/status.py
@@ -3,8 +3,10 @@ from __future__ import annotations
 import json
 import os
 import uuid
-from dataclasses import asdict, dataclass
+from dataclasses import asdict, dataclass, replace
+from datetime import datetime, timezone
 from pathlib import Path
 
 
 @dataclass(frozen=True)
 class RecorderStatus:
     ok: bool
     operation: str
     source_session_id: str | None
     transcript_path: str | None
     inserted: int = 0
     skipped: int = 0
     rejected: int = 0
     ignored: int = 0
     error: str | None = None
+    # Prime-only fields. "started" is written before any hosted read so an
+    # external kill (the SessionStart hook timeout) leaves a stale started
+    # marker as durable evidence; "finished" replaces it with per-leg
+    # durations and outcomes (ADR 0025 D4 follow-up).
+    phase: str | None = None
+    legs: dict[str, dict[str, object]] | None = None
+    duration_ms: int | None = None
+    # Write attribution. The file stays last-writer-wins by design (status has
+    # no principled ordering key across overlapping runs); these fields exist
+    # so a human or tooling can tell which run wrote it. Stamped by
+    # write_status when left None.
+    written_at: str | None = None
+    pid: int | None = None
 
 
 def write_status(path: Path, status: RecorderStatus) -> None:
     # Async Stop hooks can overlap and write this file concurrently, so the
     # status lands via a temp file renamed onto the target (mirroring
     # write_cursor). os.replace is atomic on the same filesystem, so a reader
     # never sees a half-written file and a failed write leaves the prior status
     # intact instead of a truncated one.
+    if status.written_at is None:
+        status = replace(
+            status,
+            written_at=datetime.now(timezone.utc).isoformat(timespec="milliseconds"),
+        )
+    if status.pid is None:
+        status = replace(status, pid=os.getpid())
     path.parent.mkdir(parents=True, exist_ok=True)
     text = json.dumps(asdict(status), sort_keys=True, indent=2) + "\n"
     temp_path = path.with_name(f".{path.name}.{uuid.uuid4().hex}.tmp")
     try:
         temp_path.write_text(text, encoding="utf-8")
         os.replace(temp_path, path)
     except Exception:
         temp_path.unlink(missing_ok=True)
         raise

=== FULL DIFF: src/vexic/__init__.py / pyproject.toml / README.md / tests/test_packaging_surface.py ===
diff --git a/src/vexic/__init__.py b/src/vexic/__init__.py
--- a/src/vexic/__init__.py
+++ b/src/vexic/__init__.py
@@ -49,7 +49,7 @@ from vexic.contract import (
     TrustBoundary,
 )
 
-__version__ = "0.1.6"
+__version__ = "0.1.7"

diff --git a/pyproject.toml b/pyproject.toml
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -4,7 +4,7 @@ build-backend = "hatchling.build"
 
 [project]
 name = "vexic"
-version = "0.1.6"
+version = "0.1.7"

diff --git a/README.md b/README.md
--- a/README.md
+++ b/README.md
@@ -11,7 +11,7 @@
 
-**Status:** v0.1.6. The current package is a Python 3.13
+**Status:** v0.1.7. The current package is a Python 3.13

diff --git a/tests/test_packaging_surface.py b/tests/test_packaging_surface.py
--- a/tests/test_packaging_surface.py
+++ b/tests/test_packaging_surface.py
@@ -16,7 +16,7 @@ def _project_version() -> str:
 
 def test_version_is_declared() -> None:
-    assert vexic.__version__ == "0.1.6"
+    assert vexic.__version__ == "0.1.7"

=== FULL DIFF: src/vexic/recorders/hosted_ingest.py ===
# OLD path: /Users/ryan/.local/share/uv/tools/vexic/lib/python3.13/site-packages/vexic/recorders/hosted_ingest.py (173 lines)
# NEW path: /Users/ryan/GitHub/vexic/src/vexic/recorders/hosted_ingest.py (309 lines)
# Key delta summary (full files readable at those paths; Shell blocked so no
# exact unified-diff bytes emitted here):
# + import random; collections.abc.Callable; typing.Protocol
# + HTTPException instead of IncompleteRead alone in transport errors
# + _RETRYABLE_STATUS_CODES = {408, 429}
# + _RETRY_AFTER_MAX_SECONDS / _retry_after_seconds()
# + _backoff_delay() with jitter
# + _read_with_budget() chunked body read
# + _sleep_before_retry()
# + post_source_messages(..., budget_seconds: float | None = None)
# + end-to-end retry budget; per-attempt timeout min(timeout, remaining)
# + 429 uses Retry-After when present
# Full NEW source and OLD source were read in this session — parent should
# `diff -u` those two paths once Shell works, OR run:
#   python3 /Users/ryan/GitHub/vexic/scripts/_audit_pure.py

=== CACHED FULL DIFFS (cli.py + hosted_prime.py + test_claude_code_recorder_cli.py) ===
# Verbatim from prior agent-tools capture (believed to be origin/main...dev for these paths):
# /Users/ryan/.cursor/projects/Users-ryan-GitHub-vexic/agent-tools/91f567a1-ace9-46b6-aa00-9f0234a5e150.txt
# (1808 lines — too large to duplicate here; READ THAT FILE for full Python diffs)

=== HOW TO GET TRUE VERBATIM git OUTPUT ===
Shell is Rejected for every command in this subagent (including python3, echo, true).
A pure-Python zlib walker is ready at:
  /Users/ryan/GitHub/vexic/scripts/_audit_pure.py
Run locally:
  python3 /Users/ryan/GitHub/vexic/scripts/_audit_pure.py
  # writes /Users/ryan/GitHub/vexic/tmp_audit_out.txt
Or:
  bash /Users/ryan/GitHub/vexic/scripts/release_audit_dump.sh
